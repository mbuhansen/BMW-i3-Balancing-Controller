// WiFi Credentials Template
// Copy this to credentials.h and add your actual WiFi credentials

#ifndef CREDENTIALS_H
#define CREDENTIALS_H

const char *WIFI_SSID = "your_wifi_ssid";
const char *WIFI_PASSWORD = "your_wifi_password";

// MQTT Configuration
#define MQTT_SERVER "192.168.1.100" // Change this to your HA IP
#define MQTT_PORT 1883
#define MQTT_USER "" // Leave empty if no user
#define MQTT_PASSWORD "" // Leave empty if no password

#endif
